{
  "name": "dagskra-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Remote MCP server for Icelandic TV broadcast schedules (RÚV, Sýn). Served at dagskra.9z.is.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "agents": "^0.9.0",
    "fast-xml-parser": "^4.5.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.7.2",
    "vitest": "latest",
    "wrangler": "^4.79.0"
  }
}
